server:
  port: 8080
  servlet:
    context-path: /todox
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
  data:
    web:
      pageable:
        page-serialization-mode: via-dto